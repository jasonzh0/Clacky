name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    uses: ./.github/workflows/build-workflow.yml
    with:
      upload-artifacts: false
      release: false
